<%= render 'propertyDetail' %>

<!-- if there are pending bookings on the @property>
    iterate over @property.pending_bookings
        render 
            <a href="property/:id/booking/:booking_id/approve"> #post request
            <a href="property/:id/booking/:booking_id/deny"> #post request, #write deny controller first

-->