<section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Album example</h1>
          <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
          <p>
            <a href="#" class="btn btn-primary my-2">Main call to action</a>
            <a href="#" class="btn btn-secondary my-2">Secondary action</a>
          </p>
        </div>
</section>

<div class="album py-5 bg-light">
  <div class="container">
      <div class="row">
    <% @properties.each do |property|%>
        <div class="col-md-4">
          <div class="card mb-4 box-shadow">
            <%= cl_image_tag property.property_image.url, width: "100%", height: "100%", class: "card-img-top" %>
            <div class="card-body">
              <p class="card-text"><%= property.name%></p>
              <p class="card-text"><%= property.description%></p>
              <p><%= "#{property.address.street_number} #{property.address.street_name} #{property.address.suburb}" %></p>
              <p>Bedroom(s): <%= property.bedroom_count%></p>
              <p>Bed(s): <%= property.bed_count%></p>
              <p>Bathroom(s): <%= property.bathroom_count%></p>
              <p>Price: $<%= property.price%></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to 'View', property_path(property.id), class: "btn btn-sm btn-outline-secondary" %>
                  <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                </div>
                <small class="text-muted">9 mins</small>
              </div>
            </div>
          </div>
        </div>
    <% end %>
    </div>
  </div>
</div>

